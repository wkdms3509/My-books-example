import React from 'react';
import { Switch, BrowserRouter, Router } from 'react-router-dom';

function App() {
  return (
    <BrowserRouter>
      <Switch>
        <Router />
      </Switch>
    </BrowserRouter>
  )
}

export default App;
